# default config for ddpg
name: DDPG

checkpoint_folder: checkpoints_ddpg

# cnn or lstm
network_type: lstm 

num_episodes: 100
eval_steps: 10
batch_size: 64
tau: 0.001
gamma: 0.99
buffer_size: 10000
lr_actor: 0.0001
lr_critic: 0.001
